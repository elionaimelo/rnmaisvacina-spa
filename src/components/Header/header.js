import styled from "styled-components";

export const Header = styled.header`
  h1 {
    font-weight: 800;
    font-size: 1.5rem;
  }
  .header-top {
    padding: 10px 50px;
    height: 85px;
    display: flex;
    align-items: center;
  }

  .header-bottom {
    padding: 30px 50px;
    height: auto;
  }

  .header-bottom-text {
    margin-top: 20px;
  }
`;
